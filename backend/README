# Backend
This backend is built using Python Flask and CSV files as a database. Currently hosted (port defined in main.py)
API endpoints found in core/api/controller.py

# Host and Setup server
## Python environment (optional)
Make sure Python is downloaded with pip. Then run the following commands in the backend directory:
```
python -m venv venv
venv\Scripts\Activate.ps1
pip install -r requirements.txt
```